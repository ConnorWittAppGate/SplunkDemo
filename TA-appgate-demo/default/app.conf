[install]
is_configured = 0
state = enabled

[ui]
is_visible = 1
label = AppGate ZTNA Dashboard

[launcher]
author = Appgate
description = Zero-trust telemetry dashboards for AppGate ZTNA, fed entirely by the AppGate LogForwarder audit stream. Ships the AppGate Collective Health dashboard plus the search-time field normalization it runs on. Portable: set the appgate_index macro and nothing else is environment-specific.
version = 1.2.0

[package]
id = TA-appgate-demo
